import React, { Fragment } from "react";
import "./Button.scss";

const Button = () => {
  return (
    <Fragment>
      <button className="button_btn">Add todo</button>
    </Fragment>
  );
};

export default Button;
